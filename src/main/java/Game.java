public class Game {
    private Game() {}

    public enum Colour {PURPLE, GREEN, RED};
    public enum Number {ONE, TWO, THREE}
    public enum Shape {Diamond, Oval, Squiggle}
    public enum Fill {Blank, Filled, Stripe}
}
